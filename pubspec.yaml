name: mi_primer_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.4.1
  firebase_auth: ^5.3.2
  cloud_firestore: ^5.4.2

  cupertino_icons: ^1.0.8
  provider: ^6.0.0
  shared_preferences: ^2.2.2
  permission_handler: ^12.0.1
  flutter_blue_plus: ^1.31.15
  encrypt: ^5.0.1          # AES en Dart
  flutter_secure_storage: ^8.0.0
  location: ^6.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
